/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'github-readme-stats.vercel.app', 'ghchart.rshah.org'],
  },
}

module.exports = nextConfig
